## v1.13.0
- 修复收缩末段出现“迷你展开卡片”后再瞬间刷新为普通卡片的问题。
- 根因不是最终几何坐标，而是 3× 展开布局被整体 scale 到普通卡片尺寸，导致标题/正文在末帧被一起缩小。
- 新增目标位置 compact-card hand-off：收缩最后约 100ms 将展开层淡出、原尺寸紧凑卡片淡入。
- 真实卡片在 hand-off 卡片覆盖下恢复 Grid 布局，下一帧再移除 hand-off，避免末帧跳变。

# Changelog

## v1.12.0
- 修复卡片收缩末段先缩到错误位置、再瞬间跳回原卡片的问题。
- FLIP 几何映射从 `translate + scale` 组合改为显式 `matrix3d`，避免缩放连带影响平移量。
- 展开与收缩统一使用同一套精确矩阵映射，保持动画首尾像素位置一致。

## v1.11.0
- 新增空闲慢速预读：面板打开且约 6.5 秒无操作后，以约 7.2 秒 / 条的频率逐步补齐未加载正文。
- 用户重新点击、滚动、搜索或悬停后，会取消尚未发出的空闲预读队列，避免与主动操作抢请求。
- 空闲预读继续遵守 IndexedDB 缓存、429 冷却与全局请求调度。
- 顶部“选择当前”改为“全选”，卡片创建时间去掉“创建”前缀。
- 数量菜单精简为纯数字 `10 / 30 / 50 / 100` 与“全部”。
- “加载 N 个”改为“加载 N 个对话内容”，“加载更多列表”改为“加载更多对话卡片”。
- 主面板增加从右下角启动图标原点缩放展开 / 收起的 WAAPI 动画。
- 面板打开时再次点击右下角插件图标即可丝滑缩回并关闭。

## v1.10.0
- 删除确认不再调用浏览器原生 `confirm()`，改为在所点击删除按钮旁弹出轻量确认卡片。
- 单条删除与批量删除共用同一套锚定确认交互，点击提示卡片外区域即可取消。
- 用户确认后目标卡片立即进入灰化待删除状态，不再等待网络请求结束才反馈。
- 服务端确认删除后，卡片以由右向左的粉碎 / 粉末粒子消散动画退出。
- 删除请求失败或被 429 中断时，未成功删除的卡片会恢复正常视觉状态。
- 删除完成后直接移除对应 DOM，避免每删除一条都重建整个卡片网格。

## v1.9.0
- 选中卡片在折叠状态下平滑缩小 2%，进一步强化多选反馈。
- 保持 Grid 占位尺寸不变，不影响卡片排列。
- 展开 / FLIP 动画期间恢复 100% 比例，避免影响阅读尺寸与 morph 几何。
- 为缩放加入轻量 transform 过渡。

## v1.8.0
- 重做选中卡片彩虹流光：固定边框 mask + GPU `transform: rotate()` 渐变层。
- 去除角度渐变重绘造成的闪烁感。
- 保持恒定亮度、透明度和轻微发光，不含呼吸效果。
- 避免 loaded edge 覆盖选中流光。

## v1.7.0
- 提高彩虹边缘饱和度。
- 加入恒定轻微发光。
- 禁止选中卡片叠加 loading 呼吸动画。

## v1.6.0
- 选中卡片由黑白灰流光改为低饱和度彩虹顺时针流光。

## v1.5.0
- 图片预览尺寸约束与点击空白只关闭图片。
- 已选择卡片加入动态边缘效果。

## v1.4.0
- 图片缩略图改为页面内 Lightbox 预览，不再直接触发下载。

## v1.3.0
- 性能优化：折叠卡片不再常驻完整正文 DOM。
- 展开时按需渲染 Markdown / 消息。
- 使用 `content-visibility`、搜索防抖并降低大面积 blur 成本。

## v1.2.0
- 跳转改为浏览器新标签页。
- 放大跳转 / 删除按钮。
- 增加“自动展开”开关：悬停自动模式与点击手动模式。

## v1.1.0
- 回退代理动画，恢复真实卡片 FLIP morph。
- 优化展开 / 收缩末帧与中途反向动画。

## v1.0.0
- 展开正文支持 Markdown 渲染。
- 优化滚动条。
- 支持图片缩略图与图片消息解析。
- 优化复选框、跳转和垃圾桶按钮。
- 增加慢 / 正常 / 快速加载速度菜单与快速模式限流风险确认。

## v0.9.0
- 加快卡片展开 / 收缩。
- 强化已加载卡片边缘视觉。
- 未加载正文改为灰色 Skeleton 占位线。

## v0.8.0
- 正文加载与对话列表分页加载解耦。
- “加载 N 个”改为继续加载 N 个尚未加载正文的对话。
- 重做加载数量菜单。

## v0.7.0
- 用 transform FLIP 修复从左上角弹出的展开动画问题。
- 优化收缩性能。

## v0.6.0
- 引入卡片本体 morph / FLIP 展开尝试。

## v0.5.0
- 增加“加载当前页 / 加载 N 个”批量读取能力。
- 增加醒目的 429 Too Many Requests 提示和熔断处理。
- 默认关闭激进正文预读。

## v0.4.0
- 增加 IndexedDB 正文缓存。
- 进一步降低后台预读和跨标签页请求冲突。
- 保存 429 冷却状态。

## v0.3.0
- 扩大展开卡片到约 6–9 个普通卡片面积。
- 降低详情请求并发并加入 429 退避。

## v0.2.0
- 展开卡片改为覆盖式浮层，不挤压网格。
- 增加 0.5 秒悬停延迟、对话速览和创建时间修复。
- 增加正文加载动画。

## v0.1.0
- 初始版本。
- 分批读取会话列表、卡片预览、悬停展开、卡片内滚动。
- 单条 / 批量选择、归档和删除。
